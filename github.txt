#######################
# Configurar variaveis globais
# git config --global user.name "Alex Sander Foss"
# git config --global user.email fossalex@hotmail.com
#######################

git remote add origin https://github.com/fossalex/eventex.git
git remote add origin git@github.com:fossalex/eventex.git --> não usar
git push -u origin master --> não funcionou
git push --force origin master --> foi o q funcionou